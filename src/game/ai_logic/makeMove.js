const makeMove = (board, options)=> {
// takes baord -array and options object parametrs
// from options takes dsicovered ships and finds the right position to fire next
// returns that position
};

const filterOptions = options => {
    // takes possible options and elminiates ships that are already completelty destroyed;
}